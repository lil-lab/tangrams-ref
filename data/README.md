# Experiment data

`full_data.csv` (main experiment, trial level), `demographics.csv` (de-identified participant
demographics), and `thread_contexts.csv` (stimulus design).

Every variable in these files — with units, categorical keys, and missing-data conventions — is
documented in the repository root `../README.md`, alongside the processed tables in
`../analyses/data/` and the de-identification procedure.
